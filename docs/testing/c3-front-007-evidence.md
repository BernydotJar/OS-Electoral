# C3-FRONT-007 local evidence

## Scope

Guided chapter routes, native route transitions, mission cadence and consultant-grade campaign role dossiers.

## Automated frontend evidence

```text
ESLint: PASS
TypeScript: PASS
Vitest: 23 files / 106 tests PASS
Next.js production build: PASS
npm audit: 0 vulnerabilities
```

The build registers `/{locale}/campaign/{chapter}` and enables the locked Next.js `viewTransition` experiment.

## Backend and domain evidence

```text
Ruff: PASS
Ruff format: PASS
strict mypy: PASS in 69 source files
focused team/API/model suite: 64 PASS
```

The bilingual blueprint catalog version is `2026-07-27.1`. New template roles carry decision scope, verifiable deliverables, collaboration points and operating signals. Historical documents remain compatible through empty tuple defaults. Preview additions and preserved canonical matches expose the same consulting profile and remain part of the version-bound digest.

## Browser evidence

`make frontend-e2e` passed against the production standalone build:

```text
command overview without stacked workspaces
chapter URL and selected-workspace isolation
ES → EN locale change preserving the chapter route
keyboard skip link and progressive disclosure
mission cadence: evidence → human decision → governed execution
390 × 844 reduced-motion static equivalent
zero axe WCAG 2.2 AA violations
zero horizontal overflow
empty localStorage/sessionStorage
no unexpected outbound hosts
no console or page errors
```

Artifacts are generated under `artifacts/c3-front-007-demo/` and remain ignored local evidence.

## Functional journey status

The API/PostgreSQL browser journey now covers:

```text
command overview
→ foundation route and persisted intake
→ browser back/forward
→ evidence route and source persistence
→ team route
→ lean blueprint: 5 functions with keyboard-opened dossiers
→ full preview: 5 proposed + 3 bilingual preserved, all with dossiers
→ append-only apply: 10 functions
→ manual function with complete consulting dossier: 11 functions
→ reload and chapter-by-chapter persistence
→ ES/EN/mobile/reduced-motion/axe/overflow checks
```

Local execution stopped before migrations or API startup because nested Docker cannot register the Alpine layer (`lchown /var/empty: permission denied`). Exact-head hosted CI is required for this retained proof.

## Authority findings

- Role descriptions remain organizational guidance, not authorization.
- No principal, membership, permission, capacity or onboarding state is inferred.
- No voter profile or individual political preference is stored.
- No media, content, contact, spending, mobilization or deployment action occurs.
- `authority_effect=NONE`, `external_effects=NONE`, production `BLOCKED`, release `DENY_RELEASE`.

## Exact-head hosted evidence

Implementation head `0c720d85af4c2c47daa2086f738e2eee637ae9f4` passed CampaignOS CI `30303748356` and Runtime Visual Review `30303748338` on draft PR `#124`.

The hosted PostgreSQL/API/browser job `90102796867` proved the complete route-aware sequence: command overview → foundation → browser back/forward → evidence → team → lean five-function map with keyboard-opened dossiers → full preview with five additions and three preserved bilingual matches → append-only ten-function map → manual eleventh function with persisted dossier → chapter-by-chapter ES/EN/mobile/reduced-motion review.

Retained artifacts:

- frontend review: `8667669021`, `sha256:67cd30942026413546a7d662cd796c1e3aa2251f29d3c7f26e84d61a7fcf5670`
- PostgreSQL recovery: `8667601676`, `sha256:bcec609d2aa282871fd5be3b989c4103a06303294d33546b1ec3672848fa6d71`
- supply chain: `8667588724`, `sha256:da1de30f3ce0654fc628a097afe0298db98d77ce1904715b82c012f5cce6db0e`
- visual review: `8667619538`, `sha256:843870b1ccacc9751407610e113669eff4e5b726ab00afb6f5fd10b6376d8dbe`
- Gitleaks SARIF: `8667587880`, `sha256:e8e337a70aa36798a8726bf62b21d9e2d6de26a8dc3c7418a065bd0019245a94`

Production remains `BLOCKED`, release remains `DENY_RELEASE`, and external effects remain `NONE`.
