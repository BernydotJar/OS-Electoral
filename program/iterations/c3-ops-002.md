# C3-OPS-002 iteration

## Goal

Close the repository-level rollback-runbook implementation gap with a fail-closed policy, executable validator, sanitized rehearsal receipt and usable operator decision tree.

## Approval

The user explicitly authorized completing C3-OPS-002 in SHIP mode on 2026-07-31 at 21:45 -06:00. Scope is constrained repository/local/CI implementation only. Production deployment, cloud resources, paid services, destructive database actions, image publication and political external effects remain unauthorized.

## Producer checkpoint

- cataloged all twelve Alembic revisions;
- classified destructive or control-removing downgrade paths as forward-fix only;
- allowed artifact rollback only with schema revision `20260729_0012` retained;
- added strict response selection and refusal behavior;
- added atomic sanitized evidence writing;
- added PostgreSQL 18 CI rehearsal using the existing recovery job;
- documented operator and staging handoff procedures.

## Critic focus

- false safety from downgrade code;
- mutable or missing previous-known-good artifact;
- false provenance or production claim;
- protected-control weakening;
- automatic event replay;
- secret leakage;
- restore over the source;
- unknown migration and committed-write behavior.

## Current result

Producer, red-team repairs and the complete local repository gate pass: 821 Python tests, 12 controlled skips, 90.11% coverage and 144 frontend tests. Exact-head hosted PostgreSQL 18 evidence and independent PR review also pass. The node passed exact-head CI, was squash-merged, and passed post-merge CI.

Implementation was frozen at `0cb4180d72102c413c368de467090eb3684dfe6a` before the evidence-only head.

## Exact-head CI closure

PR `#148` at head `98faf8ab1f8e614555575bd560e3e593aedb2561` passed CampaignOS CI `30683477144`, Runtime Visual Review `30683477171`, hosted actionlint and PostgreSQL 18 rollback rehearsal. Artifact `8813086739` was inspected and matches the exact head/base, migration, response and no-mutation contract. The PR has no open review findings. `C3-OPS-002` was subsequently squash-merged and post-merge verified.


## Merge closure

PR `#148` final head `b4cbcac58f94aecfe12a1f104d1717677ecbfaf7` passed all exact-head checks and was squash-merged at main `db8692d0f830496b413a8f56f2611eb611708545`. Post-merge CI `30683834955` and artifact `8813212488` pass. The repository rollback baseline is complete; managed staging and production recovery remain separate blocked gates.
